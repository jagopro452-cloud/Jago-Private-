name: jago_shared_core
description: Shared, app-agnostic code used by both customer_app and driver_app (JAGO). Path-dependency package, not published.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  google_maps_flutter: ^2.6.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
